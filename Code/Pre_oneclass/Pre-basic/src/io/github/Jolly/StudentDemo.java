package io.github.Jolly;
/*
    通常情况下，一个类并不能直接使用，需要根据类创建一个对象，才能使用
    1.导包：（也就是指出需要使用的l类，在什么位置）
    import 包名称.类名称
    import io.github.Jolly.Student
    对于和当前类属于同一包的情况下，可以省略b包语句不写
    2.创建，格式：
    类名称   对象名  =   new  类名称（）;
    Student st = new Student();
    3.使用，分两种情况：
    使用成员变量：对象名. 成员变量名
    使用成员方法：对象名. 成员方法名（参数）
    也就是，想用谁，就用对象名点谁
*/
public class StudentDemo {
}
